# 1. 프로젝트 개요
- h2에서 데이터를 읽어와서 리스트 형태로 값을 반환하는 서비스 프로젝트

# 2. 필수 기능
- 아이템 목록 조회
  - 필수 구현
      - 페이지네이션 (page, size 파라미터)
      - 검색 기능 (상품명 키워드 검색)
      - 필터링 1개 이상:
        - 상품 종류(itemType) 필터
        - 서버(server) 필터
        - 가격 범위(minPrice ~ maxPrice) 필터 중 택1 이상
      - 정렬 옵션 (등록일순, 가격순)
      - 로딩/에러/빈 결과에 대한 응답 처리
- 아이템 단건 조회
- 아이템 등록
  - 필수 구현
      - 요청 필드 검증 (Validation)
        - 필수 필드 누락 체크
        - 수량/가격은 양수만 허용
        - 상품 종류는 정의된 enum 값만 허용
      - 검증 실패 시 400 Bad Request + 상세 에러 메시지
      - 등록 성공 시 201 Created + 생성된 상품 정보 반환
- 아이템 수정
- 아이템 삭제


# 2. 기술스펙
- 목록 조회 캐싱(spring cache)
- 동시성 처리 (동일 상품 동시 수정시 문제 처리)


## 3. 추가 사항
- 테스트코드도 같이 작성해줘, 테스트코드를 통해 이 소스들을 테스트할수있어야해